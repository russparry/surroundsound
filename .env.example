# Supabase Configuration
# Get these from your Supabase project settings at https://supabase.com
# 1. Create a new project
# 2. Go to Settings > API
# 3. Copy your Project URL and anon/public key
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Configuration
# Get these from your Stripe dashboard at https://dashboard.stripe.com
# Use TEST keys for development (they start with pk_test_ and sk_test_)
# 1. Get publishable key from Developers > API keys
# 2. Get secret key from Developers > API keys
# 3. Create a product with $4.99/month subscription and copy the Price ID
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_SECRET_KEY=sk_test_your_secret_key
NEXT_PUBLIC_STRIPE_PRICE_ID=price_your_price_id

# Spotify App Credentials
# Get these from https://developer.spotify.com/dashboard
# 1. Create a new app
# 2. Copy your Client ID below
# 3. Add http://127.0.0.1:3000/auth to your app's Redirect URIs (use 127.0.0.1, not localhost)
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=your_client_id_here

# Redirect URI (must match Spotify app settings exactly)
# Use 127.0.0.1 instead of localhost (Spotify requirement)
NEXT_PUBLIC_REDIRECT_URI=http://127.0.0.1:3000/auth

# App URL
# Change this to your production URL when deploying (e.g., https://yourapp.onrender.com)
NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000

# Your local IP address (optional, for testing on other devices on your network)
# Find it by running: ipconfig (Windows) or ifconfig (Mac/Linux)
NEXT_PUBLIC_LOCAL_IP=192.168.1.xxx
